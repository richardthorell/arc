name: Build Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    name: Build Legacy
    runs-on: ubuntu-latest
    steps:
      - name: Print migration note
        run: echo "Use build-clang.yml and build-msvc.yml for regular CI."
