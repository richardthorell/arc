# ARC Assets

Small default assets used by the editor and renderer bring-up live here.

- `models/UAL2_Standard.glb` is the default startup mesh.
- `shaders/default_unlit.vert` and `shaders/default_unlit.frag` are the first default Phong mesh shader sources.

These are development assets for editor smoke testing, not the final engine asset database.
